# Package.Id

[![NuGet](https://img.shields.io/nuget/v/Package.Id)](https://www.nuget.org/packages/Package.Id/)
[![NuGet downloads](https://img.shields.io/nuget/dt/Package.Id)](https://www.nuget.org/packages/Package.Id/)
[![License](https://img.shields.io/badge/license-LGPL--3.0--or--later-blue)](https://www.gnu.org/licenses/lgpl-3.0.html)
[![CI](https://github.com/Hawkynt/RepoName/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/Hawkynt/RepoName/actions/workflows/ci.yml)

> One sentence stating what the package does and why a consumer would install it.

## 📦 Installation

```bash
dotnet add package Package.Id
```

## ✨ Features

- User-visible capability, not implementation trivia.
- Keep the bullets short; large capability sets belong in the support matrix below.

## 🧩 Support matrix

Include this section whenever support varies by format, algorithm, codec, container, filesystem,
profile, or operation. Link the **name** to a neutral overview (usually Wikipedia) and keep a
separate **Reference** column for the normative specification, original paper, or canonical
project site.

| Format / algorithm | Read / decode | Write / encode | Notes | Reference |
| --- | :---: | :---: | --- | --- |
| [Name](https://en.wikipedia.org/wiki/Example) | ✅ | ⚠️ | Supported profile or deliberate subset | [Specification](https://example.org/spec) |

`✅` full · `⚠️` partial, explain the subset in the row · `—` unsupported.

For archive and filesystem state use the repository vocabulary:

| State | Meaning |
| --- | --- |
| **R** | Read / decode / extract only. |
| **WORM** | Read plus create a fresh output, but no in-place mutation. |
| **R/W** | Read plus the modification semantics the package documents. |

## 🚀 Quick start

The smallest realistic example that proves the package's main value. It must compile against the
published surface, using the real package id.

```csharp
// ...
```

## 📚 API reference

<!-- API:BEGIN generated by Hawkynt/RepositoryTemplate/package-readme — edit the XML docs in source, not here -->
<!-- API:END -->

Leave the markers empty; CI fills and then verifies this region. Everything you write outside them
survives regeneration untouched.

Author the show-off examples as `<example>` tags on the type in the C# source. They land here and in
IntelliSense from the one place:

```csharp
/// <summary>Writes individual bits to a buffer.</summary>
/// <example>
/// <code>
/// var writer = new BitWriter(BitOrder.MsbFirst);
/// writer.WriteBits(0b1011_0010, count: 8);
/// </code>
/// </example>
public sealed class BitWriter { }
```

## 🏗️ Architecture

Optional. Package-specific sections belong here and immediately after: registry API, state model,
container/codec split, exhaustive inventories, validation matrices, implementation caveats.

## 🔌 Dependencies

Use a table when there is more than one meaningful dependency.

## ⚠️ Limitations

Deliberate subsets, read-only formats, non-interoperable encoders, platform constraints — anything a
green check in the matrix above would otherwise conceal.

## ❤️ Support

If this project saves you time or money, consider supporting its development:

[![GitHub Sponsors](https://img.shields.io/badge/GitHub-Sponsor-EA4AAA?logo=githubsponsors)](https://github.com/sponsors/Hawkynt)
[![PayPal](https://img.shields.io/badge/PayPal-Donate-00457C?logo=paypal)](https://www.paypal.me/hawkynt)

## 📜 License

Licensed under LGPL-3.0-or-later — see the repository [LICENSE](https://github.com/Hawkynt/RepoName/blob/main/LICENSE).
